# D-001: nightly. To be tightened to an exact nightly-YYYY-MM-DD once green.
[toolchain]
channel    = "nightly"
profile    = "minimal"
components = ["rust-src", "llvm-tools", "rustfmt", "clippy"]
targets    = ["riscv64gc-unknown-none-elf"]
